name: terminal
description: The default xterm for dahliaOS
publish_to: 'none'
version: 3.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  xterm: ^3.5.0
  flutter_pty: ^0.4.0
  bitsdojo_window: ^0.1.2
  zenit_ui: 
    git: 
      url: https://github.com/dahliaOS/zenit_ui.git
      ref: c549dca

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true